<section class="process" id="process">
<div class="container">
  <div class="process-hd f">
    <div class="sec-label">Как работаем</div>
    <h2>От звонка до чистого объекта</h2>
    <p>Пять шагов. На каждом — конкретный результат, который вы можете проверить.</p>
  </div>
  <div class="steps">
    <div class="step f"><div class="step-num">01</div><h3>Заявка</h3><p>Звоните, пишете в WhatsApp или оставляете заявку на сайте</p></div>
    <div class="step f"><div class="step-num">02</div><h3>Осмотр</h3><p>Выезжаем за 24 часа. Замеряем площадь, фиксируем задачи, называем цену</p></div>
    <div class="step f"><div class="step-num">03</div><h3>Договор</h3><p>Фиксированная цена, график работ, ответственность сторон. Оплата по актам</p></div>
    <div class="step f"><div class="step-num">04</div><h3>Уборка</h3><p>Бригада работает по чек-листу. Своя химия и оборудование — ничего от вас</p></div>
    <div class="step f"><div class="step-num">05</div><h3>Фотоотчёт</h3><p>Фото результата + заполненный чек-лист — в Telegram сразу после уборки</p></div>
  </div>
</div>
</section>

<style>
.process {
  background: var(--white);
  padding: clamp(80px, 10vw, 140px) 0;
  border-top: 1px solid var(--border-d);
}
.process-hd {
  max-width: 560px;
  margin-bottom: clamp(48px, 6vw, 72px);
}
.process-hd h2 {
  font-family: var(--serif);
  font-size: clamp(28px, 3.2vw, 44px);
  font-weight: 700; color: var(--navy);
  letter-spacing: -.015em; margin-bottom: 14px;
}
.process-hd p { font-size: 15px; color: var(--text-dm); line-height: 1.7; }
.steps {
  display: grid; grid-template-columns: repeat(5, 1fr);
  border-top: 1px solid var(--border-d);
}
.step {
  padding: clamp(24px, 3vw, 36px) clamp(16px, 2vw, 24px);
  border-right: 1px solid var(--border-d);
  border-bottom: 1px solid var(--border-d);
}
.step:last-child { border-right: none; }
.step-num {
  font-size: 13px; font-weight: 700;
  color: var(--teal); line-height: 1;
  margin-bottom: 14px;
  letter-spacing: .04em;
}
.step h3 {
  font-family: var(--serif);
  font-size: 17px; font-weight: 700; color: var(--navy);
  margin-bottom: 8px;
}
.step p { font-size: 13px; color: var(--text-dm); line-height: 1.6; }

@media (max-width: 960px) {
  .steps { grid-template-columns: repeat(3, 1fr); }
}
@media (max-width: 600px) {
  .steps { grid-template-columns: 1fr; }
  .step { border-right: none; }
}
</style>
