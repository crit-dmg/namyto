<section class="hero">
  <div class="hero-photo"><img src="https://plus.unsplash.com/premium_photo-1661957292212-3dfdc45303ea?fm=jpg&q=80&w=1920&auto=format&fit=crop" alt="Современный офис" loading="eager" /></div>
  <div class="container">
    <div class="hero-content">
      <div class="hero-label">Обслуживание коммерческих помещений</div>
      <h1>Чистота, которую<br>видно в отчёте</h1>
      <p class="hero-sub">Регулярная уборка офисов, складов и торговых помещений в Москве. Договор с фиксированной ценой, фотоотчёт после каждого визита.</p>
      <a href="#form" class="hero-cta">
        Обсудить обслуживание
        <svg width="18" height="18" viewBox="0 0 18 18" fill="none"><path d="M4 9h10m0 0l-4-4m4 4l-4 4" stroke="#fff" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/></svg>
      </a>
    </div>

    <div class="hero-bar">
      <div class="hero-bar-item">
        <div class="hb-label">Договор</div>
        <div class="hb-value">Фиксированная цена, оплата с НДС</div>
      </div>
      <div class="hero-bar-item">
        <div class="hb-label">Отчётность</div>
        <div class="hb-value">Фотоотчёт + чек-лист после каждой уборки</div>
      </div>
      <div class="hero-bar-item">
        <div class="hb-label">Гарантия</div>
        <div class="hb-value">Не устроило — переделаем бесплатно</div>
      </div>
      <div class="hero-bar-item">
        <div class="hb-label">Старт</div>
        <div class="hb-value">Бесплатный выезд за 24 часа</div>
      </div>
    </div>
  </div>
</section>

<style>
.hero {
  min-height: 100vh; min-height: 100svh;
  display: flex; flex-direction: column;
  justify-content: flex-end;
  padding: 0 0 clamp(80px, 10vw, 140px);
  position: relative;
}
.hero-photo {
  position: absolute; inset: 0; z-index: 0;
}
.hero-photo img { width: 100%; height: 100%; object-fit: cover; }
.hero-photo::after {
  content: '';
  position: absolute; inset: 0;
  background: linear-gradient(to top, var(--navy) 0%, rgba(26,35,50,.7) 40%, rgba(26,35,50,.4) 100%);
}
.hero-content {
  position: relative; z-index: 1;
  max-width: 680px;
  padding-top: 100px;
}
@supports (padding-top: env(safe-area-inset-top)) {
  .hero-content { padding-top: calc(100px + env(safe-area-inset-top)); }
}
.hero-label {
  font-size: 11px; font-weight: 600;
  letter-spacing: .18em; text-transform: uppercase;
  color: var(--teal);
  margin-bottom: 28px;
  display: flex; align-items: center; gap: 12px;
}
.hero-label::before {
  content: ''; width: 32px; height: 1px; background: var(--teal);
}
.hero h1 {
  font-family: var(--serif);
  font-size: clamp(36px, 4.8vw, 64px);
  font-weight: 700; line-height: 1.08;
  letter-spacing: -.02em; color: var(--white);
  margin-bottom: 24px;
  text-wrap: balance;
}
.hero-sub {
  font-size: 17px; line-height: 1.75;
  color: var(--text); max-width: 520px;
  margin-bottom: 40px;
}
.hero-cta {
  display: inline-flex; align-items: center; gap: 14px;
  padding: 18px 36px;
  background: var(--teal); color: var(--white);
  font-size: 14px; font-weight: 600; letter-spacing: .04em;
  transition: background .3s;
}
.hero-cta:hover { background: var(--teal-l); }
.hero-cta:active { transform: translateY(1px); }
.hero-cta svg { transition: transform .3s; }
.hero-cta:hover svg { transform: translateX(3px); }

.hero-bar {
  position: relative; z-index: 1;
  margin-top: clamp(48px, 6vw, 80px);
  border-top: 1px solid var(--border-l);
  display: grid; grid-template-columns: repeat(4, 1fr);
  padding-top: 28px;
}
.hero-bar-item + .hero-bar-item { padding-left: clamp(16px, 2.5vw, 32px); }
.hb-label {
  font-size: 10px; font-weight: 600;
  letter-spacing: .14em; text-transform: uppercase;
  color: var(--text-m); margin-bottom: 6px;
}
.hb-value {
  font-family: var(--serif);
  font-size: 16px; font-weight: 700;
  color: var(--white); line-height: 1.35;
}

@media (max-width: 960px) {
  .hero-bar { grid-template-columns: repeat(2, 1fr); gap: 24px 0; }
  .hero-bar-item + .hero-bar-item { padding-left: 0; }
  .hero-bar-item:nth-child(2n) { padding-left: clamp(16px, 2.5vw, 32px); }
}
@media (max-width: 600px) {
  .hero-bar { grid-template-columns: 1fr; gap: 16px; }
  .hero-bar-item + .hero-bar-item { padding-left: 0; padding-top: 16px; border-top: 1px solid var(--border-l); }
}
</style>
